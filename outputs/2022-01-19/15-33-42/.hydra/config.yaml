hyperparameters:
  epochs: 10
  print_every: 40
  lr: 0.01
  momentum: 0.9
  num_classes: 9
  TRAIN_BATCHSIZE: 64
  TEST_SIZE: 0.2
paths:
  input_filepath: ${hydra:runtime.cwd}/data/processed/
cloud:
  save: false
  path: gs://bucket-for-fishes/models
